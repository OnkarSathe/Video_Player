import { Component, signal } from '@angular/core';

@Component({
  selector: 'app-home',
  imports: [],
  templateUrl: './home.html',
  styleUrl: './home.css'
})
export class Home {
  // Reactive title to demo usage of Angular signals
  protected readonly title = signal('Home');
}
